[mongodb]
%{ for i, inst in instances ~}
node${i + 1} ansible_host=${inst.public_ip} ansible_user=ubuntu ansible_ssh_private_key_file=mongodb_cluster_key.pem private_ip=${inst.private_ip}
%{ endfor ~}
